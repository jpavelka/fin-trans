// import Plot from "react-plotly.js";
// import { getGroupedData } from './utils.js'

export const SinglePeriodPlot = ({ plotTx, metaCategory, timeFrame, time }) => {
  // const groupOn = metaCategory === "_all" ? "metaCategory" : "category";
  // const groupedData = getGroupedData({
  //   allTx: plotTx,
  //   groupOn: [groupOn, timeFrame],
  //   includeAll: [true, false],
  // });
  return <div></div>
};

export default SinglePeriodPlot